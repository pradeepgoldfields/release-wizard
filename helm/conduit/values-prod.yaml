# Production environment overrides
# Usage: helm upgrade --install conduit ./helm/conduit \
#          -f helm/conduit/values-prod.yaml \
#          --set image.tag=1.2.3 \
#          --set existingSecret=conduit-prod-secret

image:
  pullPolicy: IfNotPresent

replicaCount: 3

config:
  logLevel: WARNING
  flaskEnv: production

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 20
  targetCPUUtilizationPercentage: 65
  targetMemoryUtilizationPercentage: 75

ingress:
  host: conduit.example.com
  tls:
    enabled: true
    secretName: conduit-tls

# In production, supply a pre-created Secret (Vault, Sealed Secrets, etc.)
# and set existingSecret to its name. The 'secrets' block is then ignored.
existingSecret: "conduit-prod-secret"

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi
